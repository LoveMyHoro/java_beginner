package test_6_Object_Objects;

public class Api_Objects {
    /*
    Objects是一个工具类（），提供了一些方法去完成一些功能
    用于封装一些平时使用频率很高或容易出错的操作，这些操作形成了 Objects 的各个方法。

    1.equals():先做非空判断，比较两个对象
    底层逻辑：
    在比较前会先判断调用者是否为空，若是空，则返回false;
    若不是，则调用调用者的equals方法。
    2.isNUll():判断是否为null，若是，返回true
    3.nonNull():判断是否为null，若是，返回false
     */
}
