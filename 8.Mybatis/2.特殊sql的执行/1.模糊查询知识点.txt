方法1：直接拼接
在注解拼接时，不能使用 #{} 占位符，因为 ? 占位符会被解析为字符。应使用 ${} 进行参数替换，以优化查询。例如：
LIKE '%${username}%'

方法2：利用 CONCAT 方法
在此方法中，可以使用 #{} 占位符。示例：
LIKE CONCAT('%', #{username}, '%')

方法3：使用双引号
在模糊查询中，常用的方法是使用双引号，例如：
LIKE "%${username}%"
在这里，${username} 将被替换为实际用户的名字，有效实现模糊匹配，并避免使用 ? 占位符所带来的解析问题